---
phase: 02-high-scale-ingestion-engine
plan: 02
type: execute
wave: 1
depends_on: ["02-01"]
files_modified:
  - prisma/schema.prisma
  - src/modules/ingestion/ingestion.service.ts
  - src/modules/ingestion/processors/ingestion.processor.ts
autonomous: true
requirements: [ING-01, ING-05, COM-02]
---

<objective>
Implement Raw Event Storage for auditability and simulate sector-specific mapping (Fashion/Electronics).

Purpose: Ensure every ingestion attempt is persisted before processing (D-07) and provide a foundation for multi-sector compliance.
Output: Persistent storage of raw payloads and updated processor logic.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<tasks>

<task type="auto">
  <name>Task 1: Update Prisma Schema with RawEvent Model</name>
  <files>prisma/schema.prisma</files>
  <action>
    Add 'RawEvent' model to schema.prisma:
    - id (UUID)
    - type (String, e.g., 'SUPPLIER_DATA')
    - sector (String, e.g., 'FASHION', 'ELECTRONICS', 'BATTERY')
    - payload (Json)
    - status (String: 'PENDING', 'PROCESSED', 'FAILED')
    - jobId (String, nullable)
    - createdAt, updatedAt
  </action>
  <verify>
    <automated>npx prisma validate</automated>
  </verify>
  <done>RawEvent model is added to the schema.</done>
</task>

<task type="auto">
  <name>Task 2: Persist Raw Events in IngestionService</name>
  <files>src/modules/ingestion/ingestion.service.ts</files>
  <action>
    Update IngestionService.ingestSupplierData:
    - Determine sector from payload (default to 'BATTERY' if unspecified).
    - Create a 'RawEvent' record in PENDING status.
    - Add job to BullMQ including the rawEventId.
    - Update RawEvent with the jobId once queued.
  </action>
  <verify>
    <automated>npm test src/modules/ingestion/ingestion.service.spec.ts</automated>
  </verify>
  <done>IngestionService now persists payloads as RawEvents before queuing.</done>
</task>

<task type="auto">
  <name>Task 3: Update IngestionProcessor to Handle RawEvents</name>
  <files>src/modules/ingestion/processors/ingestion.processor.ts</files>
  <action>
    Update IngestionProcessor.process:
    - Extract rawEventId from job data.
    - Update RawEvent status to 'PROCESSED' on success.
    - Implement basic log-based sector mapping (Fashion/Electronics/Battery).
    - Update RawEvent status to 'FAILED' on error.
  </action>
  <verify>
    <automated>npm test src/modules/ingestion/processors/ingestion.processor.spec.ts</automated>
  </verify>
  <done>IngestionProcessor updates RawEvent status and handles sector logic.</done>
</task>

</tasks>

<verification>
Verify that calling the ingestion API creates a RawEvent record in the database and that the background processor updates its status to 'PROCESSED'.
</verification>

<success_criteria>
- RawEvent table exists and stores ingestion payloads.
- IngestionService persists data before queuing.
- IngestionProcessor correctly updates RawEvent status in PostgreSQL.
</success_criteria>

<output>
After completion, create `.planning/phases/02-high-scale-ingestion-engine/02-02-SUMMARY.md`
</output>
