---
phase: 02-high-scale-ingestion-engine
plan: 01
type: execute
wave: 1
depends_on: ["01-04"]
files_modified:
  - package.json
  - src/app.module.ts
  - src/modules/ingestion/ingestion.controller.ts
  - src/modules/ingestion/ingestion.service.ts
  - src/modules/ingestion/ingestion.module.ts
  - src/modules/ingestion/processors/ingestion.processor.ts
autonomous: true
requirements: [ING-01, ING-05]
user_setup:
  - service: redis
    why: "Message broker for BullMQ asynchronous processing (D-06)"
---

<objective>
Implement the High-Scale Ingestion Engine using NestJS and BullMQ to handle high-volume supplier data asynchronously.

Purpose: Scalability for large brands (H&M/Zara scale) and avoidance of ingestion bottlenecks.
Output: A functional ingestion API that offloads processing to background workers.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-high-scale-ingestion-engine/02-CONTEXT.md
@.planning/STATE.md
@.planning/ROADMAP.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Setup BullMQ and Redis Integration</name>
  <files>package.json, src/app.module.ts, src/modules/ingestion/ingestion.module.ts</files>
  <action>
    Install BullMQ and @nestjs/bullmq.
    Configure the BullModule in AppModule with Redis connection details.
    Register the 'ingestion' queue in IngestionModule.
  </action>
  <verify>
    <automated>npm list @nestjs/bullmq bullmq</automated>
  </verify>
  <done>BullMQ and Redis integration is established.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Async Ingestion Controller and Service</name>
  <files>src/modules/ingestion/ingestion.controller.ts, src/modules/ingestion/ingestion.service.ts</files>
  <action>
    Create POST /api/ingest/supplier-data endpoint.
    The service should:
    - Receive the raw supplier data payload.
    - Perform basic format validation.
    - Add the data to the 'ingestion' queue for background processing (D-06).
    - Return a 202 Accepted status with a jobId.
  </action>
  <verify>
    <automated>curl -X POST http://localhost:3000/api/ingest/supplier-data -d '{"data": "test"}' -H "Content-Type: application/json"</automated>
  </verify>
  <done>Asynchronous ingestion endpoint is functional.</done>
</task>

<task type="auto">
  <name>Task 3: Implement Ingestion Background Processor</name>
  <files>src/modules/ingestion/processors/ingestion.processor.ts</files>
  <action>
    Implement a BullMQ processor for the 'ingestion' queue.
    The processor should:
    - Log the start of processing for a jobId.
    - Simulate heavy processing (e.g., validation, mapping).
    - Log the successful completion of the job.
    - (Future: This is where D-07 Raw Event storage and sector mapping will live).
  </action>
  <verify>
    <automated>npm run test -- src/modules/ingestion/processors/ingestion.processor.spec.ts</automated>
  </verify>
  <done>Background processor correctly handles queued ingestion jobs.</done>
</task>

</tasks>

<verification>
Ensure the API returns a 202 status and a jobId, and verify through logs that the background processor picks up and completes the job.
</verification>

<success_criteria>
- Ingestion API returns 202 Accepted for valid payloads.
- BullMQ successfully queues and processes ingestion jobs.
- Redis connection is stable and configured for scalability.
</success_criteria>

<output>
After completion, create `.planning/phases/02-high-scale-ingestion-engine/02-01-SUMMARY.md`
</output>
