---
phase: 08-sustainability-scorecard
plan: 01
type: execute
wave: 1
depends_on: ["07-01"]
files_modified:
  - src/modules/gs1/gs1.module.ts
  - src/modules/gs1/passport.controller.ts
  - src/modules/gs1/scorecard.service.ts
autonomous: true
requirements: [UX-02]
---

<objective>
Implement the Sustainability Scorecard service and integrate it into the consumer passport view.

Purpose: Provide easy-to-understand sustainability metrics to consumers based on verifiable data (UX-02, D-28).
Output: A scoring service that calculates impact ratings and an updated passport API that includes the scorecard.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Scorecard Service</name>
  <files>src/modules/gs1/scorecard.service.ts</files>
  <action>
    Create 'ScorecardService' with:
    - calculateScore(productData: any, timeline: any[]): any.
    - Implement D-28 weighting logic (Carbon, Materials, Transparency).
    - Provide a grade (A-E) and numeric score (0-100).
  </action>
  <verify>
    <automated>npm test src/modules/gs1/scorecard.service.spec.ts</automated>
  </verify>
  <done>Scorecard service with weighted scoring logic is implemented.</done>
</task>

<task type="auto">
  <name>Task 2: Integrate Scorecard into Passport Controller</name>
  <files>src/modules/gs1/passport.controller.ts</files>
  <action>
    Update PassportController to:
    - Inject ScorecardService.
    - Calculate the scorecard for the requested product.
    - Include the scorecard in the final API response.
  </action>
  <verify>
    <automated>npm test src/modules/gs1/passport.controller.spec.ts</automated>
  </verify>
  <done>Consumer passport API now includes the sustainability scorecard.</done>
</task>

<task type="auto">
  <name>Task 3: Register Scorecard Components in Gs1Module</name>
  <files>src/modules/gs1/gs1.module.ts</files>
  <action>
    Register ScorecardService in Gs1Module.
  </action>
  <verify>
    <automated>curl -X GET http://localhost:3000/p/01/12345678901234/21/SN-999</automated>
  </verify>
  <done>Gs1Module updated with scorecard capabilities.</done>
</task>

</tasks>

<verification>
Verify that the passport API response now contains a 'scorecard' object with a grade, numeric score, and constituent breakdown (Carbon, Materials, Transparency).
</verification>

<success_criteria>
- ScorecardService correctly implements weighted scoring.
- Passport API returns a visually-ready scorecard for the mobile PWA.
- Missing data results in lower transparency scores as planned (D-31).
</success_criteria>

<output>
After completion, create `.planning/phases/08-sustainability-scorecard/08-01-SUMMARY.md`
</output>
