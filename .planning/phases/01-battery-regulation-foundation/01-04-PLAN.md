---
phase: 01-battery-regulation-foundation
plan: 04
type: execute
wave: 3
depends_on: ["01-02", "01-03"]
files_modified:
  - src/modules/battery/battery.controller.ts
  - src/modules/battery/battery.service.ts
  - src/modules/battery/battery.module.ts
  - test/app.e2e-spec.ts
autonomous: true
requirements: [COM-01, COM-03]
---

<objective>
Expose the battery compliance engine via a REST API and implement the initial data vault hashing pattern (D-04).

Purpose: Enable manufacturers to submit battery data for validation and storage.
Output: A functional API endpoint that validates data, calculates carbon, and saves it to the PostgreSQL vault with a hash proof.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-battery-regulation-foundation/01-01-SUMMARY.md
@.planning/phases/01-battery-regulation-foundation/01-02-SUMMARY.md
@.planning/phases/01-battery-regulation-foundation/01-03-SUMMARY.md
@.planning/phases/01-battery-regulation-foundation/01-CONTEXT.md
# Reference schemas and types
@src/modules/battery/battery.types.ts
@src/modules/battery/battery.schema.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Battery Ingestion Controller and Service</name>
  <files>src/modules/battery/battery.controller.ts, src/modules/battery/battery.service.ts, src/modules/battery/battery.module.ts</files>
  <action>
    Create a POST /api/battery/ingest endpoint.
    Accepts raw battery data payload.
    The service should:
    - Pass data to ValidationEngine (from Plan 02).
    - If valid, pass to CarbonEngine (from Plan 03) to calculate footprint.
    - Generate a SHA-256 hash of the full data (Private Data Vault hash pattern - D-04).
    - Save the record to PostgreSQL using Prisma.
    - Return the calculated footprint and validation summary.
  </action>
  <verify>
    <automated>curl -X POST http://localhost:3000/api/battery/ingest -d '{}' -H "Content-Type: application/json"</automated>
  </verify>
  <done>API is functional and integrates all compliance logic.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Full-Flow Integration Tests</name>
  <files>test/app.e2e-spec.ts</files>
  <action>
    Add E2E tests for the battery ingestion flow:
    - Valid submission returns 201 + footprint + validation report.
    - Missing critical field returns 400 + error list.
    - Valid submission with non-critical missing field returns 201 + warning list.
    - Verify that the data is correctly hashed in the database.
  </action>
  <verify>
    <automated>npm run test:e2e</automated>
  </verify>
  <done>End-to-end flow is verified and stable.</done>
</task>

</tasks>

<verification>
Perform an end-to-end check by submitting a sample battery data payload to the API and verifying its presence in the PostgreSQL database with a matching hash.
</verification>

<success_criteria>
- API endpoint successfully processes compliance data.
- Validation, calculation, and hashing logic are correctly integrated.
- Database records contain both detailed metadata and a cryptographic hash.
</success_criteria>

<output>
After completion, create `.planning/phases/01-battery-regulation-foundation/01-04-SUMMARY.md`
</output>
