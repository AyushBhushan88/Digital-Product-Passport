---
phase: 01-battery-regulation-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tsconfig.json
  - prisma/schema.prisma
  - src/app.module.ts
  - src/modules/battery/battery.schema.ts
  - src/modules/battery/battery.types.ts
autonomous: true
requirements: [COM-01, COM-03]
user_setup:
  - service: postgresql
    why: "Primary database for the Private Data Vault (D-04)"
    env_vars:
      - name: DATABASE_URL
        source: "Local or hosted PostgreSQL instance (e.g., postgresql://user:password@localhost:5432/looppass)"

must_haves:
  truths:
    - "NestJS project is initialized and running"
    - "PostgreSQL is connected via Prisma"
    - "Battery data models exist with JSON-LD context (D-02)"
  artifacts:
    - path: "prisma/schema.prisma"
      provides: "Database schema for battery passports"
    - path: "src/modules/battery/battery.schema.ts"
      provides: "JSON-LD context and schema definitions"
  key_links:
    - from: "src/modules/battery/battery.schema.ts"
      to: "JSON-LD Standard"
      via: "context definitions"
      pattern: "@context"
---

<objective>
Initialize the LoopPass NestJS project and establish the foundational data models for the Battery Passport, including the PostgreSQL database setup and JSON-LD schema definitions.

Purpose: Set the structural foundation for regulatory compliance (Feb 2025 deadline).
Output: A working NestJS API skeleton with a synchronized database schema and battery-specific data types.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-battery-regulation-foundation/01-CONTEXT.md
@.planning/research/STACK.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize NestJS Project and Prisma</name>
  <files>package.json, tsconfig.json, prisma/schema.prisma, .env</files>
  <action>
    Initialize a new NestJS project using TypeScript.
    Install core dependencies: @nestjs/config, @prisma/client, class-validator, class-transformer.
    Install dev dependencies: prisma, typescript, @types/node.
    Initialize Prisma and configure it to use PostgreSQL.
    Create a basic .env file with DATABASE_URL placeholder.
  </action>
  <verify>
    <automated>npx nest --version && npx prisma --version</automated>
  </verify>
  <done>NestJS project initialized and Prisma is ready for schema definition.</done>
</task>

<task type="auto">
  <name>Task 2: Define Battery JSON-LD Schema and Types</name>
  <files>src/modules/battery/battery.schema.ts, src/modules/battery/battery.types.ts</files>
  <action>
    Implement the hybrid JSON-LD structure per D-02.
    Define BatteryPassport types including mandatory fields for the Feb 2025 deadline:
    - Identification (serialNumber, batchID)
    - Manufacturer info
    - Carbon footprint summary
    - Material composition (cobalt, lithium, nickel, lead)
    - State of Health (SoH) placeholders.
    Use a combination of custom ontologies and Schema.org where applicable.
  </action>
  <verify>
    <automated>npx tsc src/modules/battery/battery.types.ts --noEmit</automated>
  </verify>
  <done>Core battery data types and JSON-LD context are defined.</done>
</task>

<task type="auto">
  <name>Task 3: Implement PostgreSQL Battery Registry Schema</name>
  <files>prisma/schema.prisma</files>
  <action>
    Create the 'BatteryPassport' model in schema.prisma per D-04.
    Include fields for:
    - id (UUID)
    - externalId (for GS1 Digital Link)
    - manufacturerId
    - productionDate
    - model
    - carbonFootprint (Decimal)
    - metadata (Jsonb - for flexible JSON-LD data)
    - contentHash (String - for D-04 hashing)
    - createdAt/updatedAt.
    Run 'npx prisma db push' to synchronize with the local PostgreSQL instance.
  </action>
  <verify>
    <automated>npx prisma validate</automated>
  </verify>
  <done>Database schema is defined and synchronized with PostgreSQL.</done>
</task>

</tasks>

<verification>
Ensure the NestJS application starts successfully and the Prisma client can connect to the database.
</verification>

<success_criteria>
- NestJS app starts without errors.
- Prisma schema is valid and reflects the BatteryPassport model.
- JSON-LD schemas are defined and type-safe.
</success_criteria>

<output>
After completion, create `.planning/phases/01-battery-regulation-foundation/01-01-SUMMARY.md`
</output>
