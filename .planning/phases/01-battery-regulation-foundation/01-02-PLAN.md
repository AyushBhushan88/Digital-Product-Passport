---
phase: 01-battery-regulation-foundation
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/modules/compliance/engines/validation.engine.ts
  - src/modules/compliance/engines/validation.engine.spec.ts
autonomous: true
requirements: [COM-03]
must_haves:
  truths:
    - "Critical missing fields trigger rejections"
    - "Non-critical missing fields trigger warnings"
  artifacts:
    - path: "src/modules/compliance/engines/validation.engine.ts"
      provides: "Layered validation logic (D-01)"
  key_links:
    - from: "src/modules/compliance/engines/validation.engine.ts"
      to: "Regulatory Standards"
      via: "validation rules"
      pattern: "manufacturerId|serialNumber"
---

<objective>
Implement the core Validation Engine for the Battery Passport compliance logic, following a layered approach to handle regulatory strictness (D-01).

Purpose: Ensure incoming data meets the regulatory standards for Feb 2025.
Output: A reusable validation service that classifies results as successful, flagged (warnings), or rejected (errors).
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-battery-regulation-foundation/01-01-SUMMARY.md
@.planning/phases/01-battery-regulation-foundation/01-CONTEXT.md
# Reference battery types from foundation
@src/modules/battery/battery.types.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement Layered Validation Engine</name>
  <files>src/modules/compliance/engines/validation.engine.ts, src/modules/compliance/engines/validation.engine.spec.ts</files>
  <behavior>
    - Critical missing fields (manufacturerId, serialNumber, chemistry) must trigger REJECTION.
    - Less critical missing fields (e.g., disassembly instructions) should trigger a WARNING but still return success: true.
    - Invalid data types (e.g., non-numeric carbon value) must trigger REJECTION.
    - Valid data returns success: true with an empty errors array.
  </behavior>
  <action>
    Create a 'ValidationEngine' service in NestJS.
    Define a 'validate(data: any): ValidationResult' method.
    ValidationResult should include: { success: boolean, errors: ValidationError[], warnings: ValidationWarning[] }.
    Implement logic following D-01:
    - Layer 1 (Critical): Mandatory for Feb 2025 compliance.
    - Layer 2 (Review): Recommended but not immediately blocking.
    - Layer 3 (Format): Basic JSON-LD structure check.
  </action>
  <verify>
    <automated>npm run test -- src/modules/compliance/engines/validation.engine.spec.ts</automated>
  </verify>
  <done>Validation engine correctly classifies data based on regulatory strictness.</done>
</task>

</tasks>

<verification>
Run unit tests for the ValidationEngine to confirm rejection of invalid data and warning of non-critical issues.
</verification>

<success_criteria>
- Validation service exists and is exported.
- Unit tests cover critical, non-critical, and valid cases.
- D-01 layered approach is explicitly implemented in the code.
</success_criteria>

<output>
After completion, create `.planning/phases/01-battery-regulation-foundation/01-02-SUMMARY.md`
</output>
