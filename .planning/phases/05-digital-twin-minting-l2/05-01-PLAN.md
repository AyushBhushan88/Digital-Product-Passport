---
phase: 05-digital-twin-minting-l2
plan: 01
type: execute
wave: 1
depends_on: ["04-01"]
files_modified:
  - package.json
  - src/app.module.ts
  - src/modules/ingestion/processors/ingestion.processor.ts
  - src/modules/blockchain/blockchain.service.ts
  - src/modules/blockchain/blockchain.module.ts
autonomous: true
requirements: [BCN-01, BCN-02]
---

<objective>
Implement the BlockchainService for L2 Digital Twin minting and integrate it into the ingestion pipeline.

Purpose: Provide on-chain identity for products linked to their cryptographic audit trail (BCN-01, D-17).
Output: A functional blockchain service that simulates minting (D-18) and updates product records with on-chain IDs.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<tasks>

<task type="auto">
  <name>Task 1: Setup Blockchain Module and ethers.js</name>
  <files>package.json, src/modules/blockchain/blockchain.module.ts</files>
  <action>
    Install ethers.js (v6) for blockchain interaction.
    Create a global 'BlockchainModule'.
  </action>
  <verify>
    <automated>npm list ethers</automated>
  </verify>
  <done>Ethers.js is installed and BlockchainModule is created.</done>
</task>

<task type="auto">
  <name>Task 2: Implement BlockchainService (Simulated Minting)</name>
  <files>src/modules/blockchain/blockchain.service.ts</files>
  <action>
    Create 'BlockchainService' with methods:
    - mintDigitalTwin(payloadHash: string): Promise<string> (returns a simulated transaction hash).
    - verifyOnChain(tokenId: string, expectedHash: string): Promise<boolean>.
    Implement lazy-minting logic simulation (D-18).
  </action>
  <verify>
    <automated>npm test src/modules/blockchain/blockchain.service.spec.ts</automated>
  </verify>
  <done>BlockchainService with simulated minting is implemented.</done>
</task>

<task type="auto">
  <name>Task 3: Integrate Minting into IngestionProcessor</name>
  <files>src/modules/ingestion/processors/ingestion.processor.ts</files>
  <action>
    Update IngestionProcessor to:
    - Inject BlockchainService.
    - After audit logging, trigger mintDigitalTwin using the payload hash.
    - Log the simulated on-chain ID (transaction hash).
    - (Future: Update the Registry model with the on-chain ID).
  </action>
  <verify>
    <automated>npm test src/modules/ingestion/processors/ingestion.processor.spec.ts</automated>
  </verify>
  <done>IngestionProcessor now triggers Digital Twin creation on a simulated L2.</done>
</task>

</tasks>

<verification>
Verify that processing an ingestion job results in a simulated "on-chain" minting event using the payload's cryptographic hash.
</verification>

<success_criteria>
- BlockchainService provides methods for minting and verification.
- Every successful ingestion triggers a simulated minting action.
- The payload's SHA-256 hash is correctly linked to the on-chain identity.
</success_criteria>

<output>
After completion, create `.planning/phases/05-digital-twin-minting-l2/05-01-SUMMARY.md`
</output>
