---
phase: 03-enterprise-integration-suite
plan: 02
type: execute
wave: 1
depends_on: ["03-01"]
files_modified:
  - src/modules/ingestion/ingestion.module.ts
  - src/modules/ingestion/processors/ingestion.processor.ts
  - src/modules/ingestion/connectors/connector.registry.ts
  - src/modules/ingestion/connectors/sap-s4hana.connector.ts
  - src/modules/ingestion/connectors/centric-plm.connector.ts
autonomous: true
requirements: [ING-03, ING-04]
---

<objective>
Implement standardized ERP/PLM mapping connectors for SAP S/4HANA and Centric PLM.

Purpose: Reduce integration friction by providing pre-built mapping patterns (D-10, D-11).
Output: A strategy-based connector registry that transforms external data formats into the internal LoopPass model.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Connector Strategy Registry</name>
  <files>src/modules/ingestion/connectors/connector.registry.ts</files>
  <action>
    Create a 'ConnectorRegistry' service.
    Define a 'BaseConnector' interface with a 'map(payload: any): any' method.
    Implement a way to register and retrieve connectors by name (e.g., 'SAP_S4HANA', 'CENTRIC_PLM').
  </action>
  <verify>
    <automated>npm test src/modules/ingestion/connectors/connector.registry.spec.ts</automated>
  </verify>
  <done>Connector registry is established.</done>
</task>

<task type="auto">
  <name>Task 2: Implement SAP S/4HANA and Centric PLM Connectors</name>
  <files>src/modules/ingestion/connectors/sap-s4hana.connector.ts, src/modules/ingestion/connectors/centric-plm.connector.ts</files>
  <action>
    Implement 'SapS4HanaConnector':
    - Map SAP-specific fields (e.g., MaterialNumber, CarbonFootprint_Amount) to LoopPass fields.
    Implement 'CentricPlmConnector':
    - Map Centric-specific fields (e.g., StyleCode, Composition_Percentage) to LoopPass fields.
  </action>
  <verify>
    <automated>npm test src/modules/ingestion/connectors/sap-s4hana.connector.spec.ts</automated>
  </verify>
  <done>Standardized connectors for SAP and Centric are implemented.</done>
</task>

<task type="auto">
  <name>Task 3: Integrate Connectors into IngestionProcessor</name>
  <files>src/modules/ingestion/processors/ingestion.processor.ts</files>
  <action>
    Update IngestionProcessor to:
    - Check for a 'connector' field in the payload or RawEvent metadata.
    - If present, use the ConnectorRegistry to find the appropriate strategy.
    - Apply the mapping before further processing.
  </action>
  <verify>
    <automated>npm test src/modules/ingestion/processors/ingestion.processor.spec.ts</automated>
  </verify>
  <done>IngestionProcessor now applies enterprise connector mappings.</done>
</task>

</tasks>

<verification>
Verify that passing a payload with 'connector: SAP_S4HANA' results in the data being correctly transformed according to the SAP mapping logic during processing.
</verification>

<success_criteria>
- ConnectorRegistry allows dynamic selection of mapping strategies.
- SAP S/4HANA connector successfully maps material and carbon data.
- Centric PLM connector successfully maps fashion style and composition data.
- IngestionProcessor uses connectors based on payload metadata.
</success_criteria>

<output>
After completion, create `.planning/phases/03-enterprise-integration-suite/03-02-SUMMARY.md`
</output>
