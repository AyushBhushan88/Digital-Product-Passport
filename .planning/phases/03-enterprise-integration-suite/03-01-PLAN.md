---
phase: 03-enterprise-integration-suite
plan: 01
type: execute
wave: 1
depends_on: ["02-02"]
files_modified:
  - package.json
  - src/modules/ingestion/ingestion.module.ts
  - src/modules/ingestion/bulk-ingestion.controller.ts
  - src/modules/ingestion/bulk-ingestion.service.ts
autonomous: true
requirements: [ING-02]
---

<objective>
Implement the Bulk Upload API (CSV/JSON) to allow brands to initialize product data in bulk.

Purpose: Efficiency for large-scale data onboarding (H&M/Zara scale).
Output: A multipart file upload endpoint that streams CSV/JSON data into RawEvents for asynchronous processing.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<tasks>

<task type="auto">
  <name>Task 1: Setup File Upload Dependencies</name>
  <files>package.json</files>
  <action>
    Install csv-parse for streaming CSV processing.
    Note: NestJS includes FileInterceptor by default.
  </action>
  <verify>
    <automated>npm list csv-parse</automated>
  </verify>
  <done>CSV parsing dependency is installed.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Bulk Ingestion Service</name>
  <files>src/modules/ingestion/bulk-ingestion.service.ts, src/modules/ingestion/ingestion.module.ts</files>
  <action>
    Create BulkIngestionService to:
    - Receive a File object (CSV/JSON).
    - Use streaming (csv-parse) to read records.
    - For each record, call the existing IngestionService.ingestSupplierData.
    - Provide a summary of queued items (total count).
    Register the service in IngestionModule.
  </action>
  <verify>
    <automated>npm test src/modules/ingestion/bulk-ingestion.service.spec.ts</automated>
  </verify>
  <done>BulkIngestionService correctly parses and queues records from files.</done>
</task>

<task type="auto">
  <name>Task 3: Implement Bulk Ingestion Controller</name>
  <files>src/modules/ingestion/bulk-ingestion.controller.ts, src/modules/ingestion/ingestion.module.ts</files>
  <action>
    Create POST /api/ingest/bulk endpoint.
    Use @UseInterceptors(FileInterceptor('file')) to handle multipart uploads.
    Forward the file to BulkIngestionService.
  </action>
  <verify>
    <automated>curl -X POST http://localhost:3000/api/ingest/bulk -F 'file=@test/fixtures/sample-supplier-data.csv'</automated>
  </verify>
  <done>Bulk upload endpoint is functional.</done>
</task>

</tasks>

<verification>
Verify that uploading a CSV file with multiple rows creates the corresponding number of RawEvent records and BullMQ jobs.
</verification>

<success_criteria>
- Multipart file upload handles CSV and JSON files.
- Records are streamed to minimize memory usage (D-12).
- API returns a 202 Accepted status with a count of items queued.
</success_criteria>

<output>
After completion, create `.planning/phases/03-enterprise-integration-suite/03-01-SUMMARY.md`
</output>
