---
phase: 07-consumer-scanning-provenance
plan: 01
type: execute
wave: 1
depends_on: ["06-01"]
files_modified:
  - src/app.module.ts
  - src/modules/gs1/gs1.module.ts
  - src/modules/gs1/passport.controller.ts
  - src/modules/gs1/provenance.service.ts
autonomous: true
requirements: [UX-01, UX-03]
---

<objective>
Implement the mobile-responsive Passport API and Provenance Timeline service.

Purpose: Provide consumers with a transparent view of the product's history and authenticity (UX-01, UX-03).
Output: A controller that aggregates product data and a service that generates a visual-ready provenance timeline.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Provenance Service</name>
  <files>src/modules/gs1/provenance.service.ts</files>
  <action>
    Create 'ProvenanceService' with:
    - getTimeline(gtin: string, serial: string): Promise<any[]>.
    - Aggregate events from 'RawEvent' and 'AuditLog' models.
    - Format events into a user-friendly timeline (e.g., { title: "Manufactured", date: "..." }).
  </action>
  <verify>
    <automated>npm test src/modules/gs1/provenance.service.spec.ts</automated>
  </verify>
  <done>Provenance service for timeline generation is implemented.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Passport Controller (Consumer View)</name>
  <files>src/modules/gs1/passport.controller.ts</files>
  <action>
    Create 'PassportController' with GET /p/01/:gtin/21/:serial:
    - Fetch product data and its provenance timeline.
    - Fetch on-chain status from BlockchainService.
    - Return a JSON response suitable for a mobile PWA.
  </action>
  <verify>
    <automated>npm test src/modules/gs1/passport.controller.spec.ts</automated>
  </verify>
  <done>Passport controller for consumer access is implemented.</done>
</task>

<task type="auto">
  <name>Task 3: Register Passport Components in Gs1Module</name>
  <files>src/modules/gs1/gs1.module.ts</files>
  <action>
    Register PassportController and ProvenanceService.
    Ensure necessary services (Prisma, Blockchain) are available.
  </action>
  <verify>
    <automated>curl -X GET http://localhost:3000/p/01/12345678901234/21/SN-999</automated>
  </verify>
  <done>Consumer passport endpoints are active.</done>
</task>

</tasks>

<verification>
Verify that calling the passport endpoint returns a comprehensive data object including the product details, on-chain TX hash, and a formatted provenance timeline.
</verification>

<success_criteria>
- Passport API provides a mobile-ready data structure.
- Provenance timeline correctly aggregates and formats history events.
- On-chain authenticity is clearly indicated in the response.
</success_criteria>

<output>
After completion, create `.planning/phases/07-consumer-scanning-provenance/07-01-SUMMARY.md`
</output>
