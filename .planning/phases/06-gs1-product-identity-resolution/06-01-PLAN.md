---
phase: 06-gs1-product-identity-resolution
plan: 01
type: execute
wave: 1
depends_on: ["05-01"]
files_modified:
  - src/app.module.ts
  - src/modules/ingestion/processors/ingestion.processor.ts
  - src/modules/gs1/gs1.service.ts
  - src/modules/gs1/gs1.module.ts
  - src/modules/gs1/resolver.controller.ts
autonomous: true
requirements: [BCN-03]
---

<objective>
Implement the GS1 Digital Link URI generator and a central resolver service.

Purpose: Ensure global interoperability and standardized product identification (D-21, D-22).
Output: A service for generating GS1 URIs and a controller for resolving them to internal or on-chain data.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Gs1Service for URI Generation</name>
  <files>src/modules/gs1/gs1.service.ts, src/modules/gs1/gs1.module.ts</files>
  <action>
    Create 'Gs1Service' with:
    - generateDigitalLink(gtin: string, serial: string): string (generates URI in 01/21 format).
    - validateIdentifiers(gtin: string, serial: string): boolean (basic syntax check).
    Export Gs1Service from Gs1Module.
  </action>
  <verify>
    <automated>npm test src/modules/gs1/gs1.service.spec.ts</automated>
  </verify>
  <done>Gs1Service for URI generation and validation is implemented.</done>
</task>

<task type="auto">
  <name>Task 2: Implement URI Resolver Controller</name>
  <files>src/modules/gs1/resolver.controller.ts</files>
  <action>
    Create 'ResolverController' with GET /01/:gtin/21/:serial:
    - Parse GTIN and Serial from the URL.
    - Simulate resolving them to a product twin in the registry.
    - Return a redirect (302) or a JSON-LD response (D-23).
  </action>
  <verify>
    <automated>npm test src/modules/gs1/resolver.controller.spec.ts</automated>
  </verify>
  <done>ResolverController for GS1 Digital Link URIs is implemented.</done>
</task>

<task type="auto">
  <name>Task 3: Integrate GS1 Identity into IngestionProcessor</name>
  <files>src/modules/ingestion/processors/ingestion.processor.ts</files>
  <action>
    Update IngestionProcessor to:
    - Inject Gs1Service.
    - For each twin, generate its GS1 Digital Link URI using provided identifiers (or simulated ones).
    - Log the unique product URI.
    - (Future: Store the URI in the Registry model).
  </action>
  <verify>
    <automated>npm test src/modules/ingestion/processors/ingestion.processor.spec.ts</automated>
  </verify>
  <done>IngestionProcessor now assigns standardized GS1 identities to every Digital Twin.</done>
</task>

</tasks>

<verification>
Verify that processing an ingestion job results in a generated GS1 Digital Link URI, and that calling the resolver with that URI correctly parses the identifiers.
</verification>

<success_criteria>
- Gs1Service correctly generates and validates Digital Link URIs.
- ResolverController handles standardized GS1 URI patterns.
- Every Digital Twin is assigned a globally interoperable identity.
</success_criteria>

<output>
After completion, create `.planning/phases/06-gs1-product-identity-resolution/06-01-SUMMARY.md`
</output>
